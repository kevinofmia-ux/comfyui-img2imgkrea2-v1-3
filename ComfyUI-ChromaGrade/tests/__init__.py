"""Test package for ComfyUI-ChromaGrade.

Run everything with::

    python tests/run_tests.py

The suite is plain asserts and has no third-party test dependency, because it
has to be runnable inside whatever Python environment ComfyUI happens to be
using. ``pytest tests`` also works if it is installed.
"""
